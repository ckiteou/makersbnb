<!DOCTYPE html>
<html>
  <head>
    <title>MakersBnB</title>
    <link rel="stylesheet" href="/css/piggy.css">
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">
  </head>
  <body>
    <header>
    <a href='/listings'><img id='logo' src="/images/logo.png"></a>
    <h1><% if current_user %>
    Hello, <%= current_user.first_name %></h1>
    <form action="/sessions" method="post">
      <input type="hidden" name="_method" value="delete">
      <input id='button' type="submit" value="Sign out">
    </form>
    <%else%>
    <a href='/users/login'>Sign in</a> or <a href='/users/new'>Sign up</a>
    <% end %>
    </header>
    <section id='main-section'>
      <% if flash[:notice] %>
      <%= flash[:notice] %>
      <% end %>
      <% if flash[:errors] && !flash[:errors].empty? %>
          <% flash[:errors].each do |error| %>
              <h3><%= error %></h3><br>
          <% end %>
      <% end %>
      <%= yield %>
    </section>
  </body>
</html>
